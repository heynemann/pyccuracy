As a Pyccuracy User
I want to test see select is enabled action
So that I can make sure Pyccuracy works

Scenario 1 - Checking that some page select is enabled
Given
	I go to "select_tests.htm"
When
	#The page finishes loading
Then
	I see "selEnabled" select is enabled
